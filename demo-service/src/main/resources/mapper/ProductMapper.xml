<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.demo.mapper.ProductMapper">

    <!-- 统一列清单：所有查询都用它，避免漏字段（尤其是 ownerId） -->
    <sql id="productColumns">
        <trim suffixOverrides=",">
            id,
            owner_id AS ownerId,
            title,
            description,
            price,
            images,
            category,
            status,
            view_count AS viewCount,
            reason,
            is_deleted AS isDeleted,
            create_time AS createTime,
            update_time AS updateTime
        </trim>
    </sql>
    <!-- 商品状态口径冻结：统一从枚举取 dbValue，避免硬编码漂移 -->
    <sql id="bindProductStatus">
        <bind name="PS_UNDER_REVIEW" value="@com.demo.enumeration.ProductStatus@UNDER_REVIEW.getDbValue()"/>
        <bind name="PS_ON_SALE" value="@com.demo.enumeration.ProductStatus@ON_SHELF.getDbValue()"/>
        <bind name="PS_OFF_SHELF" value="@com.demo.enumeration.ProductStatus@OFF_SHELF.getDbValue()"/>
        <bind name="PS_SOLD" value="@com.demo.enumeration.ProductStatus@SOLD.getDbValue()"/>
    </sql>


    <!-- SQL：insertProduct，insert 语句用于对应 Mapper 方法的数据访问。 -->
    <insert id="insertProduct" parameterType="com.demo.entity.Product"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO products (
            owner_id, title, description, price, images, category,
            status, view_count, reason, create_time, update_time
        ) VALUES (
                     #{ownerId}, #{title}, #{description}, #{price}, #{images}, #{category},
                     #{status}, #{viewCount}, #{reason}, NOW(), NOW()
                 )
    </insert>


    <!-- 更新商品：动态 set，谁不为 null 就更新谁；永远更新 update_time -->
    <update id="updateProduct" parameterType="com.demo.entity.Product">
        UPDATE products
        <set>
            <if test="title != null">title = #{title},</if>
            <if test="description != null">description = #{description},</if>
            <if test="price != null">price = #{price},</if>
            <if test="images != null">images = #{images},</if>
            <if test="category != null">category = #{category},</if>
            <if test="status != null">status = #{status},</if>
            <if test="reason != null">reason = #{reason},</if>

            <!-- 时间：优先用传入的 updateTime；没传就用 NOW() -->
            <if test="updateTime != null">update_time = #{updateTime},</if>
            <if test="updateTime == null">update_time = NOW(),</if>
        </set>
        WHERE id = #{id}
        AND is_deleted = 0
    </update>
    <!-- SQL：updateStatusAndReason，update 语句用于对应 Mapper 方法的数据访问。 -->
    <update id="updateStatusAndReason">
        UPDATE products
        SET status = #{status},
            reason = #{reason},
            update_time = NOW()
        WHERE id = #{id}
          AND is_deleted = 0
    </update>
    <!-- SQL：softDeleteByOwner，update 语句用于对应 Mapper 方法的数据访问。 -->
    <update id="softDeleteByOwner">
        UPDATE products
        SET is_deleted = 1,
            update_time = NOW()
        WHERE id = #{id}
          AND owner_id = #{ownerId}
          AND is_deleted = 0
    </update>
    <!-- SQL：updateStatusAndReasonByOwner，update 语句用于对应 Mapper 方法的数据访问。 -->
    <update id="updateStatusAndReasonByOwner">
        UPDATE products
        SET status = #{status},
            reason = #{reason},
            update_time = NOW()
        WHERE id = #{id}
          AND owner_id = #{ownerId}
          AND is_deleted = 0
    </update>

    <!-- SQL：updateStatusAndReasonByCurrentStatus，管理员状态迁移条件更新。 -->
    <update id="updateStatusAndReasonByCurrentStatus">
        UPDATE products
        SET status = #{targetStatus},
            reason = #{reason},
            update_time = NOW()
        WHERE id = #{id}
          AND is_deleted = 0
          AND status = #{currentStatus}
    </update>

    <!-- SQL：updateStatusAndReasonByOwnerAndCurrentStatus，卖家状态迁移条件更新。 -->
    <update id="updateStatusAndReasonByOwnerAndCurrentStatus">
        UPDATE products
        SET status = #{targetStatus},
            reason = #{reason},
            update_time = NOW()
        WHERE id = #{id}
          AND owner_id = #{ownerId}
          AND is_deleted = 0
          AND status = #{currentStatus}
    </update>

    <!--
      SQL_ID: P2S3-PROD-001
      用途: 卖家编辑商品并原子回审（状态机条件更新）
      输入: id, ownerId, currentStatus, targetStatus, title, description, price, images
      索引依赖: PRIMARY(id), idx(owner_id,status,is_deleted)
      不迁移原因: 条件更新 + 状态机原子语义强依赖 SQL 细粒度控制
    -->
    <!-- SQL：updateForEditByOwnerAndCurrentStatus，编辑字段 + 回审状态原子更新。 -->
    <update id="updateForEditByOwnerAndCurrentStatus">
        UPDATE products
        SET title = #{title},
            description = #{description},
            price = #{price},
            <if test="images != null">images = #{images},</if>
            status = #{targetStatus},
            reason = #{reason},
            update_time = NOW()
        WHERE id = #{id}
          AND owner_id = #{ownerId}
          AND is_deleted = 0
          AND status = #{currentStatus}
    </update>

    <!-- SQL：updateStatusAndReasonIfUnderReview，update 语句用于对应 Mapper 方法的数据访问。 -->
    <update id="updateStatusAndReasonIfUnderReview">
        <include refid="bindProductStatus"/>
        UPDATE products
        SET status = #{status},
            reason = #{reason},
            update_time = NOW()
        WHERE id = #{productId}
          AND is_deleted = 0
          AND status = #{PS_UNDER_REVIEW}
    </update>

    <!-- 后台：待审核商品列表（动态筛选）
         规则：不传 status 时默认只查 under_review，符合“待审核”语义 -->
    <select id="getPendingApprovalProducts" resultType="com.demo.entity.Product">
        <include refid="bindProductStatus"/>
        SELECT
        <include refid="productColumns"/>
        FROM products
        <where>
            AND is_deleted = 0
            <if test="productName != null and productName != ''">
                AND title LIKE CONCAT('%', #{productName}, '%')
            </if>
            <if test="category != null and category != ''">
                AND category = #{category}
            </if>

            <choose>
                <when test="status == null or status == ''">
                    AND status = #{PS_UNDER_REVIEW}
                </when>
                <when test="status == '全部'">
                    <!-- 查询全部状态，不添加 status 条件 -->
                </when>
                <otherwise>
                    AND status = #{status}
                </otherwise>
            </choose>

        </where>
        ORDER BY create_time DESC
    </select>

    <!-- 根据商品 ID 查询（必须带 ownerId，否则权限校验必炸） -->
    <select id="getProductById" resultType="com.demo.entity.Product">
        SELECT
        <include refid="productColumns"/>
        FROM products
        WHERE id = #{productId}
        AND is_deleted = 0
        LIMIT 1
    </select>

    <!-- 查询用户自己的商品列表 -->
    <select id="getUserProducts" resultType="com.demo.entity.Product">
        SELECT
        <include refid="productColumns"/>
        FROM products
        WHERE owner_id = #{userId}
        AND is_deleted = 0
        <if test="status != null and status != '' and status != '全部'">
            AND status = #{status}
        </if>
        ORDER BY create_time DESC
    </select>

   <!--
     SQL_ID: P2S3-PROD-002
     用途: 市场商品检索（全文检索 + 条件过滤 + 相关性排序）
     输入: keyword, category
     索引依赖: FULLTEXT(ft_title_desc_ngram), idx_products_status(status,is_deleted,create_time)
     不迁移原因: FULLTEXT/MATCH 排序策略与执行计划强相关，MP Wrapper 可读性与可控性不如 XML
   -->
   <!-- SQL：getMarketProductList，select 语句用于对应 Mapper 方法的数据访问。 -->
   <select id="getMarketProductList" resultType="com.demo.entity.Product">
    <include refid="bindProductStatus"/>
    SELECT <include refid="productColumns"/>
    FROM products p
       WHERE p.status = #{PS_ON_SALE}
       AND p.is_deleted = 0
    <if test="keyword != null and keyword != ''">
        AND MATCH(p.title, p.description) AGAINST(#{keyword} IN NATURAL LANGUAGE MODE)
    </if>
    <if test="category != null and category != ''">
        AND p.category = #{category}
    </if>

    <choose>
        <when test="keyword != null and keyword != ''">
            ORDER BY MATCH(p.title, p.description) AGAINST(#{keyword} IN NATURAL LANGUAGE MODE) DESC,
                     p.create_time DESC
        </when>
        <otherwise>
            ORDER BY p.create_time DESC
        </otherwise>
    </choose>
</select>


    <!-- SQL：getMarketProductById，select 语句用于对应 Mapper 方法的数据访问。 -->
    <select id="getMarketProductById" resultType="com.demo.entity.Product">
        <include refid="bindProductStatus"/>
        SELECT <include refid="productColumns"/>
        FROM products p
        WHERE p.id = #{productId}
        AND p.status = #{PS_ON_SALE}
        AND p.is_deleted = 0
        LIMIT 1
    </select>


    <!--
      SQL_ID: P2S3-PROD-003
      用途: 卖家商品状态分布聚合统计
      输入: sellerId
      索引依赖: idx(owner_id,status)
      不迁移原因: 多 CASE 聚合统计 SQL 可读性在 XML 中更稳定，迁移收益低
    -->
    <!-- SQL：countProductsBySellerId，select 语句用于对应 Mapper 方法的数据访问。 -->
    <select id="countProductsBySellerId" resultType="com.demo.dto.seller.SellerProductCountDTO">
        <include refid="bindProductStatus"/>

        SELECT
        CAST(COUNT(*) AS SIGNED) AS totalProducts,
        CAST(COALESCE(SUM(CASE WHEN status = #{PS_UNDER_REVIEW} THEN 1 ELSE 0 END), 0) AS SIGNED) AS underReviewProducts,
        CAST(COALESCE(SUM(CASE WHEN status = #{PS_ON_SALE}      THEN 1 ELSE 0 END), 0) AS SIGNED) AS onSaleProducts,
        CAST(COALESCE(SUM(CASE WHEN status = #{PS_OFF_SHELF}    THEN 1 ELSE 0 END), 0) AS SIGNED) AS offShelfProducts,
        CAST(COALESCE(SUM(CASE WHEN status = #{PS_SOLD}         THEN 1 ELSE 0 END), 0) AS SIGNED) AS soldProducts
        FROM products
        WHERE owner_id = #{sellerId}
    </select>

    <!-- SQL：countActiveProductsByOwnerId，select 语句用于对应 Mapper 方法的数据访问。 -->
    <select id="countActiveProductsByOwnerId" resultType="java.lang.Long">
        <include refid="bindProductStatus"/>
        SELECT CAST(COUNT(*) AS SIGNED)
        FROM products
        WHERE owner_id = #{ownerId}
        AND is_deleted = 0
        AND status IN (#{PS_UNDER_REVIEW}, #{PS_ON_SALE})
    </select>

    <!-- SQL：listByIds，select 语句用于对应 Mapper 方法的数据访问。 -->
    <select id="listByIds" resultType="com.demo.entity.Product">
        select id, title, price, images, status
        from products
        where id in
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </select>

    <!--
      SQL_ID: P2S3-PROD-004
      用途: 指定日期商品发布量分组统计
      输入: date
      索引依赖: idx(create_time,is_deleted), idx(category,create_time)
      不迁移原因: DATE 函数 + GROUP BY 聚合语义需要显式 SQL 优化空间，先保留 XML
    -->
    <!-- Day13 Step7 - 统计指定日期的商品发布量（按 category 分组） -->
    <select id="countProductPublishByDate" resultType="com.demo.dto.statistics.ProductPublishCountDTO">
        SELECT
            category,
            COUNT(*) AS count
        FROM products
        WHERE DATE(create_time) = #{date}
          AND is_deleted = 0
        GROUP BY category
    </select>

    <select id="countByStatusAndDate" resultType="long">
        SELECT COUNT(1)
        FROM products
        WHERE status = #{status}
          AND DATE(create_time) = #{date}
          AND is_deleted = 0
    </select>

</mapper>
