<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.demo.mapper.ProductStatusAuditLogMapper">

    <!-- SQL：insert，写入商品状态审计日志。 -->
    <insert id="insert" parameterType="com.demo.entity.ProductStatusAuditLog" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO product_status_audit_log (
            product_id,
            action,
            operator_id,
            operator_role,
            before_status,
            after_status,
            reason_code,
            reason_text,
            extra_json,
            create_time
        ) VALUES (
            #{productId},
            #{action},
            #{operatorId},
            #{operatorRole},
            #{beforeStatus},
            #{afterStatus},
            #{reasonCode},
            #{reasonText},
            #{extraJson},
            NOW()
        )
    </insert>
</mapper>

