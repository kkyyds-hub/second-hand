# UserFrontDay02 联调准备与验收

- 日期：`2026-03-23`
- 文档版本：`v1.1`
- 当前状态：`已正式接棒（文档接棒完成，待进入实现 / 联调）`

---

## 1. 环境前置条件

1. 继续沿用 Day01 的用户端主工程、登录态与 `authentication` 请求头基线；
2. 后端 `UserMeController`、`AddressController` 所依赖服务可用；
3. 若后续进入头像上传，需要额外确认本地文件存储 / 上传链路可用；
4. 本线程尚未执行这些动作，因此这里只冻结验收路径，不生成通过结论。

---

## 2. Day02 首个最小切片验证清单

| 场景 | 计划验证点 | 预期留证 |
|---|---|---|
| 进入账户中心 | 登录后进入 `AccountCenterPage`，仍能看到既有 session 快照 | 页面截图、基础页面记录 |
| 资料编辑表单 | 昵称 / 简介编辑入口可打开并提交 | 页面截图、提交前后表单记录 |
| `PATCH /user/me/profile` 提交 | 请求成功返回用户资料结果 | 请求与响应结果 |
| session 回写 | patch 成功后本地 `user_profile` 与页面展示同步更新 | 页面截图、localStorage 或调试记录 |
| 鉴权回归 | Day02 页面仍遵守 Day01 登录态与 401 规则 | 路由跳转记录、异常说明 |

> 注意：以上只是 Day02 第一刀的最小验收路径，不代表头像上传、安全绑定、地址 CRUD 已一并通过。

---

## 3. Day02 后续子流验证清单

| 场景 | 计划验证点 | 预期留证 |
|---|---|---|
| 头像上传 | 上传配置、实际上传、资料回写三段式完整 | 页面截图、接口结果、失败说明 |
| 密码修改 | 修改成功 / 失败提示都可记录 | 请求与响应结果、失败提示截图 |
| 手机号 / 邮箱绑定解绑 | 成功 / 失败 / 重复提交路径清晰 | 页面截图、接口结果、异常说明 |
| 地址列表与默认地址 | 地址分页与默认地址展示一致 | 页面截图、接口结果 |
| 地址新增 / 编辑 / 删除 | 表单提交流程完整，失败态不丢失 | 页面截图、接口结果、失败提示 |