# UserFrontDay02 联调准备与验收

- 日期：`2026-03-18`
- 文档版本：`v1.0`
- 当前状态：`计划中（v1.0 已建档，执行未开始）`

---

## 1. 环境前置条件

1. 继续沿用 Day01 的用户端主工程与登录态基线；
2. 后端 `UserMeController`、`AddressController` 所依赖服务可用；
3. 若执行头像上传，需要确认本地文件存储 / 上传链路可用；
4. 本线程尚未执行这些动作，因此这里只冻结验收路径，不生成通过结论。

---

## 2. Day02 最小验证清单

| 场景 | 计划验证点 | 预期留证 |
|---|---|---|
| 账户资料编辑 | 编辑昵称 / 简介 / 头像后，页面展示与本地 session 回写一致 | 页面截图、接口结果、回填记录 |
| 密码修改 | 修改成功 / 失败提示都可记录 | 请求与响应结果、失败提示截图 |
| 手机号 / 邮箱绑定解绑 | 成功 / 失败 / 重复提交路径清晰 | 页面截图、接口结果、异常说明 |
| 地址列表与默认地址 | 地址分页与默认地址展示一致 | 页面截图、接口结果 |
| 地址新增 / 编辑 / 删除 | 表单提交流程完整，失败态不丢失 | 页面截图、接口结果、失败提示 |
| 鉴权回归 | Day02 新页面仍遵守 Day01 登录态与 401 规则 | 路由跳转记录、401 行为说明 |

---

## 3. 当日需要保留的证据

- `npm.cmd run build` 或等价构建命令结果；
- 关键页面截图：账户中心、地址列表、地址表单、默认地址切换；
- 至少一组成功 / 失败接口结果；
- 若头像上传涉及临时票据或上传 key，需在回填中说明其来源与限制。

---

## 4. 验收判定建议

1. 若仅完成文档或代码骨架，状态仍应保持 `计划中` 或 `进行中`；
2. 若实现完成但没有运行证据，只能写 `代码已完成待运行验证`；
3. 只有真实执行并回填后，才能写 `已完成并回填`；
4. 若账户资料或地址链路受接口缺口影响，应明确写 `阻塞`，不能虚报通过。

---

## 5. 当前已知待验证点

- 头像上传真实请求形态；
- 资料编辑后本地 session 的同步策略；
- 绑定解绑场景的验证码 / 校验渠道差异；
- 地址新增 / 编辑表单字段与 `AddressDTO` 的最终映射。
