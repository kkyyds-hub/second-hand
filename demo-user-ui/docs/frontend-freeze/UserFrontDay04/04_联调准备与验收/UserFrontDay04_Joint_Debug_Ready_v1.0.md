# UserFrontDay04 联调准备与验收

- 日期：`2026-03-18`
- 文档版本：`v1.0`
- 当前状态：`计划中（v1.0 已建档，执行未开始）`

---

## 1. 环境前置条件

1. Day01 的登录态、路由守卫、request 头注入保持可用；
2. `SellerController`、`UserProductController` 相关服务可访问；
3. 若商品表单依赖更复杂上传或附件能力，需要在执行时再补充前置条件；
4. 本线程尚未执行这些动作，因此这里只冻结验收路径，不生成通过结论。

---

## 2. Day04 最小验证清单

| 场景 | 计划验证点 | 预期留证 |
|---|---|---|
| 卖家工作台摘要 | 摘要卡片加载、导航入口清晰 | 页面截图、接口结果、回填记录 |
| 商品列表与详情 | 列表加载、进入详情、空态 / 错误态可记录 | 页面截图、接口结果 |
| 商品创建 / 编辑 / 删除 | 表单提交流程完整，成功 / 失败路径都可记录 | 页面截图、接口结果、失败提示 |
| 状态流转 | 下架、重新提交、兼容 `on-shelf`、撤回路径清晰 | 页面截图、接口结果、状态说明 |
| 鉴权回归 | 卖家域页面仍遵守 Day01 登录态与 401 规则 | 路由跳转记录、401 行为说明 |

---

## 3. 当日需要保留的证据

- `npm.cmd run build` 或等价构建命令结果；
- 卖家工作台、商品列表、商品表单、状态按钮与结果提示截图；
- 至少一组商品创建 / 编辑 / 状态操作接口结果；
- 若 `on-shelf` 与 `resubmit` 表现差异引发问题，需要保留语义说明和实际结果。

---

## 4. 验收判定建议

1. 仅完成文档或代码骨架时，状态仍应保持 `计划中` 或 `进行中`；
2. 有代码无运行证据时，只能写 `代码已完成待运行验证`；
3. `on-shelf` 未被真实验证前，不得写成“直接上架链路已通过”；
4. 只有真实执行并回填后，才能写 `已完成并回填`。

---

## 5. 当前已知待验证点

- 卖家工作台独立路由与导航设计；
- 商品列表查询字段与筛选口径；
- 商品创建 / 编辑表单字段映射；
- `off-shelf / resubmit / on-shelf / withdraw` 四条状态路径的真实回显与提示语。
