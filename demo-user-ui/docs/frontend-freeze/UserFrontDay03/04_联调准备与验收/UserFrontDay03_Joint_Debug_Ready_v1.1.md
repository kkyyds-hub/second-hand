# UserFrontDay03 联调准备与验收

- 日期：`2026-04-17`
- 文档版本：`v1.1`
- 当前状态：`输入准备完成（待执行线程产出 runtime 证据）`

---

## 1. 联调前置条件（执行线程）

1. Day02 仍保持“待最终裁定”口径，不在 Day03 线程重写 Day02 结论。
2. Day01 登录态、路由守卫、request 共享层保持可用。
3. 相关 controller 可达：`MarketProductController`、`FavoriteController`、`ReviewController`。

---

## 2. 必须验证项（执行线程必须覆盖）

1. 列表加载：分页/筛选/空态/错误态。
2. 详情加载：列表跳详情、详情与评论列表分层加载。
3. 收藏链路：状态查询、收藏、取消收藏、失败恢复。
4. 收藏列表：展示、跳详情、空态。
5. 举报链路：成功、失败、重复提交提示。
6. 鉴权回归：未登录跳转、401 清理行为不回归。

---

## 3. 可后置项（允许先不做）

1. 评论创建提交闭环（若受订单前置限制，可先保留入口并标记阻塞）。
2. 跨域大回归（Day09 统一承接）。

---

## 4. 验收状态书写规则

1. 未跑 runtime：写 `代码已完成待运行验证`。
2. 跑了且通过：写 `已完成并回填`，并附证据路径。
3. 跑了但受前置限制：写 `阻塞`，并标明 owner/reason/next action。

---

## 5. 证据最小清单（执行线程回填）

1. `npm.cmd run build` 结果摘要。
2. 列表/详情/收藏/举报最小链路截图或结构化结果。
3. 至少一组收藏与举报请求结果。
4. 阻塞项的接口路径与触发条件。
