# UserFrontDay03 联调准备与验收

- 日期：`2026-03-18`
- 文档版本：`v1.0`
- 当前状态：`计划中（v1.0 已建档，执行未开始）`

---

## 1. 环境前置条件

1. Day01 的登录态、路由守卫、request 头注入保持可用；
2. `MarketProductController`、`FavoriteController`、`ReviewController` 相关服务可访问；
3. 本线程尚未执行这些动作，因此这里只冻结验收路径，不生成通过结论。

---

## 2. Day03 最小验证清单

| 场景 | 计划验证点 | 预期留证 |
|---|---|---|
| 市场列表 | 列表加载、分页 / 筛选切换、空态 / 错误态可记录 | 页面截图、接口结果、回填记录 |
| 商品详情 | 从列表进入详情，详情数据与评论列表可区分加载 | 页面截图、接口结果 |
| 收藏动作 | 收藏 / 取消收藏 / 收藏状态切换路径明确 | 页面截图、接口结果、失败提示 |
| 收藏列表 | 收藏夹页能展示与跳转 | 页面截图、接口结果 |
| 举报动作 | 举报成功 / 失败 / 重复提交都能留证 | 页面截图、接口结果、异常说明 |
| 我的评价入口 / 评论提交 | 若启用则记录真实结果；若受订单前置限制则明确标记阻塞 | 结果截图或阻塞说明 |
| 鉴权回归 | 市场域页面仍遵守 Day01 登录态与 401 规则 | 路由跳转记录、401 行为说明 |

---

## 3. 当日需要保留的证据

- `npm.cmd run build` 或等价构建命令结果；
- 市场列表、详情页、收藏夹、举报对话与评论区截图；
- 至少一组收藏与举报接口结果；
- 若评论提交被阻塞，需要保留阻塞依据与对应接口 / 业务前置说明。

---

## 4. 验收判定建议

1. 仅完成文档或代码骨架时，状态仍应保持 `计划中` 或 `进行中`；
2. 有代码无运行证据时，只能写 `代码已完成待运行验证`；
3. 评论提交若受前置依赖限制，不得因为列表页可见就整体写 `已完成并回填`；
4. 只有真实执行并回填后，才能写 `已完成并回填`。

---

## 5. 当前已知待验证点

- 市场列表查询字段与筛选口径；
- 收藏状态与详情页联动方式；
- 举报动作的失败态与重复提交策略；
- 评论提交是否依赖订单完成、以及该依赖是否需要拆到后续 Day。
