# UserFrontDay08 范围冻结（Scope Freeze）

- 日期：`2026-03-18`
- 文档版本：`v1.0`
- 当前状态：`计划中（v1.0 已建档，执行未开始）`
- 当日主题：`共享交互、错误治理、系统通知与 API 模块整治`

---

## 1. 起点结论

- 结论：`UserFrontDay08 应作为 共享交互、错误治理、系统通知与 API 模块整治 的正式入口。`
- 判定依据：
  1. 当 Day02~Day07 的业务域逐步明确后，用户端需要一个独立治理日来统一交互规则、错误处理与跨域 API 模块边界。
  2. 后端已存在 `MessageController` 的未读数 / 系统通知入口，而前端尚无通知中心、统一 badge 与错误治理基线。
  3. 若不先建立 Day08 入口，系统通知、全局提示、空态 / 错误态和 API 模块整治会散落到各业务线程里。

---

## 2. 做什么

1. 冻结系统通知 / 未读数、共享 loading / empty / error / submit 规则、跨域 API 模块命名与页面消费边界，以及 Day09 之前必须统一的共享治理口径。

---

## 3. 不做什么

1. 不重开 Day02~Day07 的业务语义
2. 不把系统通知写成完整消息中心已完成
3. 不把 Day08 文档当成 Day09 运行通过的替代品。

---

## 4. 范围拆解

| 范围分类 | 归属内容 | 处理方式 |
|---|---|---|
| 系统通知 / 未读数 | `MessageController` 通知链路 | 作为 Day08 主链。 |
| 共享交互与错误治理 | loading / empty / error / submit / retry 规则 | 作为 Day08 主链。 |
| 共享 API 模块整治 | orders / afterSales / messages / wallet / points / credit 等边界 | 作为 Day08 主链。 |
| 沿用基线 | Day01 request/auth 规则、Day06 order chat 边界 | 只做接棒说明。 |
| 非 Day08 范围 | 真实联调通过、演示冻结移交 | 留给 Day09 / Day10 处理。 |

---

## 5. 未来验收口径（退出标准）

> 以下口径只有在真正进入执行后才需要满足；当前 v1.0 仅完成计划建档。

1. 系统通知、共享交互、API 模块整治三组子流都已有明确 owner、规则口径、回填入口和代表性验证清单。
2. 所有未实现、待验证、阻塞项都必须诚实留在 Day 内回填，不提前写成“已完成”。
3. 构建、运行、联调状态都必须回写到 `05_进度回填`。

---

## 6. 证据口径

- 当前已具备的证据：`MessageController.java`、`src/utils/request.ts`、Day08 六文档骨架。
- 当前尚未具备的证据：Day08 相关通知页面、共享交互规则落地、跨域 API 模块整治结果、构建结果、运行态或联调结果。
- 因此当前只能写到 `文档已记录`，不能写成 `代码已确认 + 构建已通过` 或 `联调已通过`。
