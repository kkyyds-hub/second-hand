# UserFrontDay08 前后端接口对齐

- 日期：`2026-03-18`
- 文档版本：`v1.0`
- 当前状态：`计划中（v1.0 已建档，执行未开始）`

---

## 1. 对齐目标

围绕 `共享交互、错误治理、系统通知与 API 模块整治` 冻结本日需要承接的接口入口和规则口径，并明确当前还没有任何对应前端实现落地。

---

## 2. 接口清单

| 场景 | 接口 / 契约 | 计划口径 | 备注 |
|---|---|---|---|
| 未读数 | `GET /user/messages/unread-count` | 作为通知 badge 与全局角标入口。 | 订单会话本身不在 Day08 内重复定义。 |
| 系统通知列表 | `GET /user/messages/system-notices` | 作为通知列表页入口。 | 执行时补分页、筛选和空态规则。 |
| 系统通知详情 | `GET /user/messages/system-notices/{messageId}` | 作为通知详情入口。 | 执行时补详情字段与已读联动规则。 |
| 系统通知全部已读 | `PUT /user/messages/system-notices/read` | 作为通知批量已读入口。 | 执行时补操作确认与失败态说明。 |
| 共享错误治理 | 跨域列表 / 表单 / 提交失败规则（前端规则项） | 作为 Day08 的共享约束。 | 以 Day01 request 规则和 Day02~Day07 页面边界为基础。 |

---

## 3. 对齐原则

1. 先引用前序 Days 已冻结边界，再定义本日新增的治理或收口口径。
2. 继续复用 Day01 的鉴权、401 与 `authentication` 请求头规则。
3. 所有失败态、空态、阻塞说明都要保留在文档与后续回填里。
4. 字段级不确定项一律写成“待执行时确认”。

---

## 4. 已知缺口 / 边界说明

- 当前还没有对应页面、路由、运行证据或 handoff 结果。
- 当前只有控制器 / 规则入口与计划文档，尚无 build / runtime / 联调证据。
- 若执行中发现接口或规则语义变化，必须先更新本文档再进入实现线程。
