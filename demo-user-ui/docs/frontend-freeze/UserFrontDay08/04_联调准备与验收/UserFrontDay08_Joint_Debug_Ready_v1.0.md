# UserFrontDay08 联调准备与验收

- 日期：`2026-03-18`
- 文档版本：`v1.0`
- 当前状态：`计划中（v1.0 已建档，执行未开始）`

---

## 1. 环境前置条件

1. Day01 的登录态、路由守卫、request 头注入保持可用。
2. 相关控制器、规则入口或前序 Day 文档可访问。
3. 本线程尚未执行这些动作，因此这里只冻结验收路径，不生成通过结论。

---

## 2. 最小验证清单

| 场景 | 计划验证点 | 预期留证 |
|---|---|---|
| 系统通知 / 未读数 | badge、列表、详情、全部已读路径清晰 | 页面截图、接口结果、状态说明 |
| 共享列表状态 | 空态、loading、error、retry 规则可记录 | 页面截图、规则说明、失败样例 |
| 共享表单提交状态 | 提交 loading、成功提示、失败提示与按钮禁用可记录 | 页面截图、规则说明 |
| API 模块整治 | 代表性模块的页面消费边界与错误映射可回查 | 文档记录、代码路径、失败说明 |
| 鉴权回归 | 通知页和共享页面仍遵守 Day01 登录态与 401 规则 | 路由跳转记录、401 行为说明 |

---

## 3. 验收判定建议

1. 仅完成文档或代码骨架时，状态仍应保持 `计划中` 或 `进行中`。
2. 有代码无运行证据时，只能写 `代码已完成待运行验证`。
3. 只有真实执行并回填后，才能写 `已完成并回填`。
