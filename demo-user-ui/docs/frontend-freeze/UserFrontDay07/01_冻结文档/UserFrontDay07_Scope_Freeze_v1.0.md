# UserFrontDay07 范围冻结（Scope Freeze）

- 日期：`2026-03-18`
- 文档版本：`v1.0`
- 当前状态：`计划中（v1.0 已建档，执行未开始）`
- 当日主题：`钱包、积分与信用资产视图`

---

## 1. 起点结论

- 结论：`UserFrontDay07 应作为 钱包、积分与信用资产视图 的正式入口。`
- 判定依据：
  1. 钱包、积分与信用视图通常依赖 Day05 / Day06 的订单状态与履约结果，因此需要放在订单域之后独立建档。
  2. 后端已存在 `WalletController`、`PointsController`、`UserCreditController`，而用户端还没有资产中心、流水页或信用页入口。
  3. 若不把资产域单独建档，钱包、积分、信用很容易被混进账户中心、订单日或最终演示日。

---

## 2. 做什么

1. 冻结钱包余额 / 流水 / 提现、积分总额 / 流水、信用分概览 / 流水的契约、API 模块、路由与回填口径。

---

## 3. 不做什么

1. 不把真实银行 / 支付渠道提现写成已打通
2. 不回写 Day05 支付结果或 Day06 履约逻辑
3. 不把资产页存在提前写成财务链路已联调通过。

---

## 4. 范围拆解

| 范围分类 | 归属内容 | 处理方式 |
|---|---|---|
| 钱包 | 余额、流水、提现 | 作为 Day07 主链。 |
| 积分 | 总额与流水 | 作为 Day07 主链。 |
| 信用视图 | 信用概览与信用流水 | 作为 Day07 主链。 |
| 沿用基线 | Day01、Day05、Day06 结论 | 只做接棒说明。 |
| 非 Day07 范围 | 系统通知、共享治理、联调回归、移交 | 留给 Day08 以后处理。 |

---

## 5. 未来验收口径（退出标准）

> 以下口径只有在真正进入执行后才需要满足；当前 v1.0 仅完成计划建档。

1. 钱包、积分、信用三组子流都已有明确 owner、接口口径、回填入口和风险说明。
2. 所有未实现、待验证、阻塞项都必须诚实留在 Day 内回填，不提前写成“已完成”。
3. 构建、运行、联调状态都必须回写到 `05_进度回填`。

---

## 6. 证据口径

- 当前已具备的证据：`WalletController.java`、`PointsController.java`、`UserCreditController.java`、Day07 六文档骨架。
- 当前尚未具备的证据：Day07 相关前端页面、API 模块、构建结果、运行态或联调结果。
- 因此当前只能写到 `文档已记录`，不能写成 `代码已确认 + 构建已通过` 或 `联调已通过`。
