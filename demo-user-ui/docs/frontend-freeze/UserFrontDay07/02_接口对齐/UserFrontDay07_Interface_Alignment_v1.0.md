# UserFrontDay07 前后端接口对齐

- 日期：`2026-03-18`
- 文档版本：`v1.0`
- 当前状态：`计划中（v1.0 已建档，执行未开始）`

---

## 1. 对齐目标

围绕 `钱包、积分与信用资产视图` 冻结本日需要承接的接口入口，并明确当前还没有任何对应前端实现落地。

---

## 2. 接口清单

| 场景 | 接口 / 契约 | 计划口径 | 备注 |
|---|---|---|---|
| 钱包余额 | `GET /user/wallet/balance` | 作为钱包余额页主入口。 | 执行时补精度与空态说明。 |
| 钱包流水 | `GET /user/wallet/transactions` | 作为钱包流水列表入口。 | 执行时补分页与筛选口径。 |
| 提现申请 | `POST /user/wallet/withdraw` | 作为提现申请入口。 | 执行时补表单校验与失败态。 |
| 积分总额 / 流水 | `GET /user/points/total`、`GET /user/points/ledger` | 作为积分页入口。 | 执行时补账户中心摘要与独立页边界。 |
| 信用概览 / 流水 | `GET /user/credit`、`GET /user/credit/logs` | 作为信用页入口。 | 执行时补等级文案与字段映射。 |

---

## 3. 对齐原则

1. API 模块先分域规划，不把不同角色链路混写。
2. 继续复用 Day01 的鉴权、401 与 `authentication` 请求头规则。
3. 所有失败态、空态、阻塞说明都要保留在文档与后续回填里。
4. 字段级不确定项一律写成“待执行时确认”。

---

## 4. 已知缺口 / 边界说明

- 当前还没有对应页面、路由与 API 模块实现。
- 当前只有控制器与计划文档，尚无 build / runtime / 联调证据。
- 若执行中发现接口语义变化，必须先更新本文档再进入实现线程。
