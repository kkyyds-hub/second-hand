# UserFrontDay09 前后端接口对齐

- 日期：`2026-03-18`
- 文档版本：`v1.0`
- 当前状态：`计划中（v1.0 已建档，执行未开始）`

---

## 1. 对齐目标

围绕 `真实联调、回归与证据补齐` 冻结本日需要承接的接口入口和规则口径，并明确当前还没有任何对应前端实现落地。

---

## 2. 接口清单

| 场景 | 接口 / 契约 | 计划口径 | 备注 |
|---|---|---|---|
| 鉴权 / 路由链路 | Day01 的登录、注册、激活、401、退出、UserLayout | 作为 Day09 首批回归链路。 | 以 Day01 已冻结口径为准。 |
| 账户 / 市场 / 卖家链路 | Day02~Day04 涉及的控制器与页面 | 作为 Day09 分域回归链路。 | 仅验证已实现内容，未实现项必须保留阻塞。 |
| 订单 / 售后 / 消息链路 | Day05~Day08 涉及的 `OrdersController`、`AfterSaleController`、`MessageController` | 作为 Day09 重点联调链路。 | 订单会话、系统通知、售后和支付需分开记录结果。 |
| 资产链路 | `WalletController`、`PointsController`、`UserCreditController` | 作为 Day09 资产域回归链路。 | 未实现项不得因为进入 Day09 就被写成已通过。 |
| 共享治理链路 | Day08 的共享交互、错误治理与 API 模块整治规则 | 作为 Day09 代表性页面回归基线。 | 不新增控制器，只验证规则落地结果。 |

---

## 3. 对齐原则

1. 先引用前序 Days 已冻结边界，再定义本日新增的治理或收口口径。
2. 继续复用 Day01 的鉴权、401 与 `authentication` 请求头规则。
3. 所有失败态、空态、阻塞说明都要保留在文档与后续回填里。
4. 字段级不确定项一律写成“待执行时确认”。

---

## 4. 已知缺口 / 边界说明

- 当前还没有对应页面、路由、运行证据或 handoff 结果。
- 当前只有控制器 / 规则入口与计划文档，尚无 build / runtime / 联调证据。
- 若执行中发现接口或规则语义变化，必须先更新本文档再进入实现线程。
