# UserFrontDay09 范围冻结（Scope Freeze）

- 日期：`2026-03-18`
- 文档版本：`v1.0`
- 当前状态：`计划中（v1.0 已建档，执行未开始）`
- 当日主题：`真实联调、回归与证据补齐`

---

## 1. 起点结论

- 结论：`UserFrontDay09 应作为 真实联调、回归与证据补齐 的正式入口。`
- 判定依据：
  1. Day01~Day08 已把用户端的业务域与共享治理边界规划出来，但没有 Day09，就无法把这些规划真正转成可回查的运行证据。
  2. Day09 不新增新的业务域 owner，而是负责把前序 Days 的实现、阻塞、运行结论和证据目录统一收口。
  3. 若跳过 Day09 直接进入 Day10，演示版冻结与移交将缺少真实 build / runtime 依据。

---

## 2. 做什么

1. 冻结 Day01~Day08 关键链路的 build / run / blocker / runtime-artifacts 回填路径、回归矩阵、阻塞分级和证据目录口径。

---

## 3. 不做什么

1. 不把 Day09 当成隐藏新功能实现的兜底日
2. 不在没有真实证据时把前序 Days 全部升级成联调已通过
3. 不绕过 `05_进度回填` 直接写根 README。

---

## 4. 范围拆解

| 范围分类 | 归属内容 | 处理方式 |
|---|---|---|
| 关键链路联调 | Day01~Day08 的 auth、账户、市场、卖家、订单、资产、通知与共享治理链路 | 作为 Day09 主链。 |
| 运行证据目录 | runtime-artifacts、build 日志、截图、接口结果、阻塞说明 | 作为 Day09 主链。 |
| 矩阵同步收口 | 矩阵与根 README 状态同步 | 作为 Day09 主链。 |
| 沿用基线 | Day01~Day08 已冻结的接口与验收口径 | 只做接棒说明。 |
| 非 Day09 范围 | 演示版冻结与移交 | 留给 Day10 处理。 |

---

## 5. 未来验收口径（退出标准）

> 以下口径只有在真正进入执行后才需要满足；当前 v1.0 仅完成计划建档。

1. Day01~Day08 的关键链路都有明确的 build / runtime / blocker 记录入口，矩阵状态与各 Day 回填保持一致，并能直接被 Day10 引用。
2. 所有未实现、待验证、阻塞项都必须诚实留在 Day 内回填，不提前写成“已完成”。
3. 构建、运行、联调状态都必须回写到 `05_进度回填`。

---

## 6. 证据口径

- 当前已具备的证据：Day09 六文档骨架、Day01~Day08 的现有计划文档。
- 当前尚未具备的证据：统一的用户端 runtime-artifacts 目录、真实构建与运行记录、跨日回归结论。
- 因此当前只能写到 `文档已记录`，不能写成 `代码已确认 + 构建已通过` 或 `联调已通过`。
