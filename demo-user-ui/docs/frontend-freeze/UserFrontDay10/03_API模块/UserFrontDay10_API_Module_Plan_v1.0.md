# UserFrontDay10 API 模块规划

- 日期：`2026-03-18`
- 文档版本：`v1.0`
- 当前状态：`计划中（v1.0 已建档，执行未开始）`

---

## 1. 模块目标

把 `演示版冻结与移交` 落到清晰的 API 模块、页面消费边界、证据目录或 handoff 引用规则上。

---

## 2. 重点文件（规划态）

| 文件 | 当前状态 | 角色 | 说明 |
|---|---|---|---|
| `README.md` | 已存在 | 作为 Day10 的全局 handoff 主入口 | Day10 执行时需同步最终冻结结论。 |
| `00_Business_Coverage_Matrix.md` | 已存在 | 作为 Day10 的跨日覆盖账本 | Day10 执行时需同步演示 / 非演示 / 阻塞结论。 |
| `UserFrontDay01~09/*` | 已存在 / 部分待执行 | 作为 Day10 的证据与结论来源 | Day10 只引用已确认条目，不重写事实。 |
| `UserFrontDay10/04_联调准备与验收/` | 已建档 | 承接最终演示清单与 handoff 验收项 | 当前线程只冻结口径，不创建执行证据。 |
| `UserFrontDay10/05_进度回填/` | 已建档 | 承接最终冻结、遗留项与冻结后维护回填 | 当前线程只建规划，不写已完成。 |

---

## 3. API / 资料规则

1. 先分域或分层规划，不把不同角色、不同阶段的链路混写。
2. 字段适配、错误映射、证据目录或 handoff 引用优先在本日文档里收口。
3. 未经真实执行，不把规划文件写成已完成实现或已完成 handoff。

---

## 4. 当前字段与适配口径

- 正式演示链路与不演示链路的划分待执行时确认。
- 最终 handoff 资料目录、链接检查与冻结后维护说明待 Day09 后补齐。
- 若 Day10 启动时发现 Day09 证据不足，必须先回到 Day09 或对应业务日补证据。
