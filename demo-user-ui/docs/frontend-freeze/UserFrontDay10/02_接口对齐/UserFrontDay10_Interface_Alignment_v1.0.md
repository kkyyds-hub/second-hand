# UserFrontDay10 前后端接口对齐

- 日期：`2026-03-18`
- 文档版本：`v1.0`
- 当前状态：`计划中（v1.0 已建档，执行未开始）`

---

## 1. 对齐目标

围绕 `演示版冻结与移交` 冻结本日需要承接的接口入口和规则口径，并明确当前还没有任何对应前端实现落地。

---

## 2. 接口清单

| 场景 | 接口 / 契约 | 计划口径 | 备注 |
|---|---|---|---|
| Day01~Day09 已确认接口集合 | 以各 Day `02_接口对齐` 与 `05_进度回填` 为准 | 作为 Day10 的引用依据。 | Day10 不新增接口，只引用已确认结论。 |
| 演示主链接口集合 | 最终由 Day09 真实证据筛选出的可演示链路 | 作为 Day10 的演示范围输入。 | 当前还不能写成已确定通过。 |
| 非演示 / 阻塞接口集合 | Day09 标记为阻塞、未实现、非演示的接口链路 | 作为 Day10 的遗留与风险输入。 | 不能因为进入 Day10 就被自动隐藏。 |
| handoff 入口 | `README.md`、`00_Business_Coverage_Matrix.md`、各 Day `05_进度回填` | 作为 Day10 的最终引用目录。 | 需要保持引用关系稳定、不歧义。 |

---

## 3. 对齐原则

1. 先引用前序 Days 已冻结边界，再定义本日新增的治理或收口口径。
2. 继续复用 Day01 的鉴权、401 与 `authentication` 请求头规则。
3. 所有失败态、空态、阻塞说明都要保留在文档与后续回填里。
4. 字段级不确定项一律写成“待执行时确认”。

---

## 4. 已知缺口 / 边界说明

- 当前还没有对应页面、路由、运行证据或 handoff 结果。
- 当前只有控制器 / 规则入口与计划文档，尚无 build / runtime / 联调证据。
- 若执行中发现接口或规则语义变化，必须先更新本文档再进入实现线程。
