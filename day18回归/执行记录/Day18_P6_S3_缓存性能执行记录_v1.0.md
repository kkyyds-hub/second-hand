# Day18 P6-S3 缓存性能执行记录 v1.0

- 日期：2026-02-26
- 关联复现文档：`day18回归/执行复现步骤/Day18_P6_S3_缓存性能与失效策略_执行复现_v1.0.md`
- 当前状态：文档化 + 动态验证已完成（2026-02-27）。

---

## 1. 环境信息

1. 服务地址：`http://localhost:8080`
2. Redis：本地 Redis（DB=2）
3. 执行人：`Codex`
4. 执行时间：`2026-02-27`
5. 证据来源：运行态接口调用 + Redis 实测 + JSON 归档

---

## 2. 本次完成内容（运行态）

1. 商品详情缓存命中验证通过：
   - `cache:product:detail:920012:v1` 在首次查询后存在（`EXISTS=1`）。
   - TTL 实测值有效（示例：`141s`）。
2. 商品列表缓存落地验证通过：
   - 按代码规则计算列表 key：
     `cache:product:list:2:f6a93b4721ffa1ec474935b77dc2390c0328ecba0ba80e620215c55464fcae6d:v1`
   - 实测存在（`EXISTS=1`），TTL 有效（示例：`22s`，受抖动影响）。
3. 写后失效验证通过（`afterCommit`）：
   - 卖家下架后列表版本 `2 -> 3`（`cache:product:list:version` 递增）。
   - 同时详情键被删除（`detailExistsAfterWrite=0`）。
4. 穿透/击穿基础防护演练通过：
   - 不存在商品 `999999991` 两次查询均返回业务失败（`code=0`）。
   - Redis 生成空值键 `cache:product:detail:999999991:v1`，值为 `__NULL__`，TTL=`20s`。
   - 并发 5 次同一不存在商品请求后，空值键仍有效（TTL 递减正常）。

---

## 3. DoD 回填

| DoD 项 | 结果 | 说明 |
|---|---|---|
| 已完成关键缓存键 TTL 与现状行为回归 | `[x]` | 详情缓存键与 TTL 实测通过 |
| 已完成写后失效行为验证（成功链路） | `[x]` | 下架后详情删键 + 列表 version 递增 |
| 已完成关键读路径缓存策略核对 | `[x]` | 列表/详情缓存 key 与策略一致 |
| 已完成穿透/击穿防护演练（接入后） | `[x]` | 空值缓存与并发场景基础演练通过 |

---

## 4. 证据文件

1. `day18回归/执行记录/Day18_P6_S3_动态验证结果_2026-02-27_10-07-49.json`
2. `day18回归/执行记录/Day18_P6_S3_防护演练结果_2026-02-27_10-09-22.json`

---

## 5. 备注

1. 列表缓存 TTL 受抖动策略影响，不固定为单一值（示例出现 `22s` 属正常波动）。
2. 本次仅验证缓存链路；Outbox 残留 FAIL 事件属于异步链路问题，需按 Outbox 手册单独处置。

---

（文件结束）
