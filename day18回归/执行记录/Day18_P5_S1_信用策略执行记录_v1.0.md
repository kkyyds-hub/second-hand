# Day18 P5-S1 信用策略执行记录 v1.0

- 日期：2026-02-25
- 关联复现文档：`day18回归/执行复现步骤/Day18_P5_S1_信用评分与等级限制_执行复现_v1.0.md`
- 当前状态：已完成（静态核验 + 动态验证回填）。

---

## 1. 环境信息

1. 服务地址：`http://localhost:18080`
2. 数据库：`secondhand2`
3. 执行人：`Codex`
4. 执行时间：`2026-03-04 10:53`
5. 原始证据：`day18回归/执行记录/Day18_CloseLoop_Dynamic_Result_2026-03-04_10-53-17.json`

---

## 2. 静态核验结论（已完成）

1. 信用重算公式、常量与等级阈值一致。
2. 来源字段已完成表级与字段级追溯。
3. 卖家限制已确认落在 `createProduct`：LV1 阻断、LV2 限额、LV3+ 放行。

---

## 3. 动态执行回填表

| 场景 | 输入 | 预期 | 实际结果 | 是否通过 |
|---|---|---|---|---|
| A 来源字段追溯 | `POST /admin/credit/recalc?userId=2` + SQL 汇总 | 可解释重算结果 | SQL 汇总：`completed_as_seller=19`,`violation_credit_sum=-110`,`admin_adjust_sum=62`；重算后 `score=90, level=lv3`，与来源口径一致 | `[x]` |
| B LV1 阻断 | 调整到 `lv1` 后 `POST /user/products` | 返回 LV1 阻断错误 | 调整后等级 `lv1`，创建商品返回 `code=0`，错误语义命中“LV1 暂不可发布商品” | `[x]` |
| C LV2 限额 | 调整到 `lv2` 后连续创建 4 次 | 前 3 成功，第 4 次失败 | 先清空活跃商品到 `0`；第 `1~3` 次 `code=1` 成功，第 `4` 次 `code=0`，错误语义命中“LV2 上限 3” | `[x]` |
| D LV3+ 放行 | 调整到 `lv3` 后创建商品 | 创建成功 | 调整后等级 `lv3`，创建商品 `code=1` 成功 | `[x]` |

---

## 4. DoD 勾选（回填区）

- [x] 信用分来源字段可追溯（静态核验已完成）。
- [x] 限制策略与等级映射一致（LV1 阻断 / LV2 限额 / LV3+ 放行均已动态验证）。
- [x] 执行证据归档完成（已归档 2026-03-04 动态验证结果）。

---

（文件结束）
