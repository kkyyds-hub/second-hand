# Day18 P3-S3 CSRF 策略评审执行记录 v1.0

- 日期：2026-02-25
- 关联复现文档：`day18回归/执行复现步骤/Day18_P3_S3_CSRF边界与请求防伪策略_执行复现_v1.0.md`
- 当前状态：已执行并完成回填。

---

## 1. 环境信息

1. 服务地址：`http://localhost:8080`
2. 执行人：`Codex`
3. 执行时间：`2026-02-25 14:58:26 ~ 14:59:09`
4. 原始证据：`day18回归/执行记录/Day18_P3_S3_接口防伪边界验证结果_2026-02-25_14-59-09.json`

---

## 2. 场景执行结果

| 场景 | 操作 | 预期 | 实际结果 | 是否通过 |
|---|---|---|---|---|
| A1 静态核查 | 拦截器范围与豁免路径扫描 | `/user/**`、`/admin/**` 边界清晰 | 已确认：`/admin/employee/login`、`/user/auth/**` 被豁免 | `[x]` |
| A2 静态核查 | token 头配置扫描 | 管理端/用户端 token header 固定 | 已确认：`token` / `authentication` | `[x]` |
| B1 运行态 | 无 token 调受保护写接口 | 返回 401 | `POST /user/orders`、`PATCH /user/me/profile`、`POST /admin/orders/1/flags` 均 401 | `[x]` |
| B2 运行态 | 无 token 调匿名白名单写接口 | 非 401，可进入业务层 | `/user/auth/login/password` 200；`/admin/employee/login` 200（业务拒绝非管理员）；`/payment/callback` 200 | `[x]` |

---

## 3. 关键结果摘录

1. 受保护写接口无 token 统一被拦截为 `401`。
2. 匿名白名单写接口可达业务层，证明“鉴权豁免边界”与文档一致。
3. `POST /payment/callback` 在匿名可达前提下仍由业务签名/时间戳逻辑控制流程，符合“匿名回调接口必须防伪”的策略口径。

---

## 4. DoD 勾选（回填区）

- [x] 团队对 CSRF 边界与处置策略无歧义。  
- [x] 安全评审可按文档直接判定接口风险。  
- [x] 已形成执行证据（静态扫描 + 运行态边界验证 + 原始结果文件）。  

---

（文件结束）
