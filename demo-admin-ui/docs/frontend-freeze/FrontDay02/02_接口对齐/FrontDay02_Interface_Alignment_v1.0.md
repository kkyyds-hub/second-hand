# FrontDay02 前后端接口对齐

- 日期：`2026-03-11`
- 文档版本：`v1.0`
- 当前状态：`已完成并回填`

---

## 1. 对齐目标

围绕 `用户与商家管理主链路冻结`，固定当天前端需要依赖的接口、字段与已知缺口。

---

## 2. 接口清单

| 场景 | 接口 / 契约 | 当前口径 | 备注 |
|---|---|---|---|
| 用户列表 | `GET /admin/user` | 支持分页、关键字、角色、状态等查询。 | 当前作为用户管理主查询接口。 |
| 人工建档 | `POST /admin/user` | 用于后台人工创建用户/商家档案。 | 创建成功后前端应刷新列表。 |
| 封禁用户 | `PUT /admin/user/{userId}/ban?reason=...` | 通过 query 传 reason。 | 前端已有封禁理由弹窗。 |
| 解封用户 | `PUT /admin/user/{userId}/unban` | 解除已限制账号。 | 前端应回刷列表状态。 |
| 导出用户 | `GET /admin/user/export` | 导出能力已登记到接口文档。 | 是否已在页面暴露入口需后续再核对。 |

---

## 3. 当日接口对齐原则

1. 优先引用真实接口文档与代码中的固定实现。
2. 已知后端字段缺口必须写入备注，不做乐观省略。
3. 接口变化后，应同步更新对应 API 模块文档和进度回填结论。

---

## 4. 当前已知风险 / 缺口

1. **运行态导出链路验证**：导出接口已登记，但当前冻结体系里还没有运行态结果。
2. **分页/筛选固定截图归档**：建议在 FrontDay09 做统一联调证据沉淀。
