# FrontDay01 前后端接口对齐

- 日期：`2026-03-10`
- 文档版本：`v1.0`
- 当前状态：`已完成并回填`

---

## 1. 对齐目标

围绕 `文档治理基线、登录与路由冻结`，固定当天前端需要依赖的接口、字段与已知缺口。

---

## 2. 接口清单

| 场景 | 接口 / 契约 | 当前口径 | 备注 |
|---|---|---|---|
| 管理员登录 | `POST /admin/employee/login` | 请求体 `{ loginId, password }`，成功后写入本地 token。 | 已在 `src/api/auth.ts` 和 `src/utils/request.ts` 中成链。 |
| Token 请求头 | `自定义请求头 `token`` | 不是 `Authorization: Bearer`，所有管理端请求沿用统一拦截器。 | 属于固定协议，不能在文档里写错。 |
| 未登录回跳 | `前端路由守卫` | 访问受保护页且本地无 token 时强制回 `/login`。 | 由 `src/router/index.ts` 控制。 |
| 退出登录 | `本地 token 清理 + 跳转 `/login`` | 当前以前端本地清理为准，未接单独后端退出接口。 | 属于已冻结前端口径。 |

---

## 3. 当日接口对齐原则

1. 优先引用真实接口文档与代码中的固定实现。
2. 已知后端字段缺口必须写入备注，不做乐观省略。
3. 接口变化后，应同步更新对应 API 模块文档和进度回填结论。

---

## 4. 当前已知风险 / 缺口

1. **登录链路运行态截图和固定回归记录**：当前缺少统一截图或执行记录，后续可在 FrontDay09 补运行证据。
