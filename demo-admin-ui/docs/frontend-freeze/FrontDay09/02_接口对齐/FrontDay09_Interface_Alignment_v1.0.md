# FrontDay09 前后端接口对齐

- 日期：`2026-03-18`
- 文档版本：`v1.0`
- 当前状态：`计划中`

---

## 1. 对齐目标

围绕 `联调回归与问题清零冻结`，固定当天前端需要依赖的接口、字段与已知缺口。

---

## 2. 接口清单

| 场景 | 接口 / 契约 | 当前口径 | 备注 |
|---|---|---|---|
| 登录链路 | `POST /admin/employee/login + token 协议` | 回归登录、401、退出。 | 需要运行态证据。 |
| 首页链路 | `/admin/dashboard/overview + /admin/statistics/*` | 确认页面真实数据覆盖情况。 | 需要运行态证据。 |
| 用户链路 | `GET/POST/PUT /admin/user*` | 回归查询、建档、封禁、解封。 | 需要运行态证据。 |
| 商品审核链路 | `GET/PUT /admin/products/*` | 回归列表、通过、驳回。 | 需要运行态证据。 |
| 纠纷链路 | `GET /admin/audit/overview + 两类处理接口` | 回归详情与处理。 | 需要运行态证据。 |
| 运维链路 | `/admin/ops/*` | 回归查询和至少一项动作。 | 需要运行态证据。 |

---

## 3. 当日接口对齐原则

1. 若接口尚未稳定，先写清缺口和兜底策略，再决定是否接入。
2. 运行态未验证时，不把“理论可接”写成“已完成”。
3. 接口变化后，应同步更新对应 API 模块文档和进度回填结论。

---

## 4. 当前已知风险 / 缺口

1. **跨页面真实联调**：等待 Day09 执行。
