# FrontDay03 前后端接口对齐

- 日期：`2026-03-12`
- 文档版本：`v1.0`
- 当前状态：`已完成并回填`

---

## 1. 对齐目标

围绕 `商品审核与设置占位冻结`，固定当天前端需要依赖的接口、字段与已知缺口。

---

## 2. 接口清单

| 场景 | 接口 / 契约 | 当前口径 | 备注 |
|---|---|---|---|
| 商品审核列表 | `GET /admin/products/pending-approval` | 前端当前以 `page=1&pageSize=100` 拉取，再做页面层展示。 | 真实接口已登记。 |
| 审核通过 | `PUT /admin/products/{productId}/approve` | 直接发起通过操作。 | 前端已有动作入口。 |
| 审核驳回 | `PUT /admin/products/{productId}/reject` | 请求体需要 `{ reason }`。 | 驳回原因弹窗已接入。 |
| 设置页 | `当前无固定真实接口` | 系统设置页仍是结构占位，接口规划后移。 | 必须在文档中写清楚。 |

---

## 3. 当日接口对齐原则

1. 优先引用真实接口文档与代码中的固定实现。
2. 已知后端字段缺口必须写入备注，不做乐观省略。
3. 接口变化后，应同步更新对应 API 模块文档和进度回填结论。

---

## 4. 当前已知风险 / 缺口

1. **商品审核字段完整性运行态确认**：卖家名、价格、风险等级仍依赖后端字段质量，当前仅能确认前端兜底存在。
2. **系统设置真实配置项定义**：该部分留到 FrontDay06 进一步收口。
